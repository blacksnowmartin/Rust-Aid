Make sure you run on Linux based build environment.

Run following commmands if you are running on WSL to build seamlessly.

```bash
sudo apt-get update
```
```bash
sudo apt install build-essential
```
